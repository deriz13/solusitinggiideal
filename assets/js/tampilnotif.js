var myVar = setInterval(myTimer, 3000);

function myTimer() {
    $.toast({
        text: "Baru saja memesan paket Sgrow", // Text that is to be shown in the toast
        heading: 'Ilham di Sumedang', // Optional heading to be shown on the toast
        icon: 'success', // Type of toast icon
        showHideTransition: 'slide', // fade, slide or plain
        allowToastClose: true, // Boolean value true or false
        hideAfter: 7000, // false to make it sticky or number representing the miliseconds as time after which toast needs to be hidden
        stack: 5, // false if there should be only one toast at a time or a number representing the maximum number of toasts to be shown at a time

        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 }, // bottom-left or bottom-right or bottom-center or top-left or top-right or top-center or mid-center or an object representing the left, right, top, bottom values


        textAlign: 'left', // Text alignment i.e. left, right or center
        loader: true, // Whether to show loader or not. True by default
        loaderBg: '#e22d2d', // Background color of the toast loader




    });
    clearInterval(myVar);
}




var myVar2 = setInterval(myTimer2, 10000);

function myTimer2() {
    $.toast({
        text: "Baru Saja memesan paket Sgrow", // Text that is to be shown in the toast
        heading: 'Dewi Kusnaini di Jember', // Optional heading to be shown on the toast
        icon: 'success', // Type of toast icon
        showHideTransition: 'slide', // fade, slide or plain
        allowToastClose: true, // Boolean value true or false
        hideAfter: 7000, // false to make it sticky or number representing the miliseconds as time after which toast needs to be hidden
        stack: 5, // false if there should be only one toast at a time or a number representing the maximum number of toasts to be shown at a time

        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 }, // bottom-left or bottom-right or bottom-center or top-left or top-right or top-center or mid-center or an object representing the left, right, top, bottom values


        textAlign: 'left', // Text alignment i.e. left, right or center
        loader: true, // Whether to show loader or not. True by default
        loaderBg: '#e22d2d', // Background color of the toast loader




    });
    clearInterval(myVar2);
}



var myVar3 = setInterval(myTimer3, 25000);

function myTimer3() {
    $.toast({
        text: "13m yang lalu memesan paket Sgrow", // Text that is to be shown in the toast
        heading: 'Icha Sumiati di Bandung', // Optional heading to be shown on the toast
        icon: 'success2', // Type of toast icon
        showHideTransition: 'slide', // fade, slide or plain
        allowToastClose: true, // Boolean value true or false
        hideAfter: 7000, // false to make it sticky or number representing the miliseconds as time after which toast needs to be hidden
        stack: 5, // false if there should be only one toast at a time or a number representing the maximum number of toasts to be shown at a time

        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 }, // bottom-left or bottom-right or bottom-center or top-left or top-right or top-center or mid-center or an object representing the left, right, top, bottom values


        textAlign: 'left', // Text alignment i.e. left, right or center
        loader: true, // Whether to show loader or not. True by default
        loaderBg: '#e22d2d', // Background color of the toast loader




    });
    clearInterval(myVar3);
}



var myVar4 = setInterval(myTimer4, 35000);

function myTimer4() {
    $.toast({
        text: "18m yang lalu memesan paket Sgrow", // Text that is to be shown in the toast
        heading: 'Ayu Salsabila di Solo', // Optional heading to be shown on the toast
        icon: 'success2', // Type of toast icon
        showHideTransition: 'slide', // fade, slide or plain
        allowToastClose: true, // Boolean value true or false
        hideAfter: 7000, // false to make it sticky or number representing the miliseconds as time after which toast needs to be hidden
        stack: 5, // false if there should be only one toast at a time or a number representing the maximum number of toasts to be shown at a time

        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 }, // bottom-left or bottom-right or bottom-center or top-left or top-right or top-center or mid-center or an object representing the left, right, top, bottom values


        textAlign: 'left', // Text alignment i.e. left, right or center
        loader: true, // Whether to show loader or not. True by default
        loaderBg: '#e22d2d', // Background color of the toast loader




    });
    clearInterval(myVar4);
}



var myVar5 = setInterval(myTimer5, 45000);

function myTimer5() {
    $.toast({
        text: "22m yang lalu memesan paket Sgrow", // Text that is to be shown in the toast
        heading: 'Sandi Setyawan di Sulawesi', // Optional heading to be shown on the toast
        icon: 'success', // Type of toast icon
        showHideTransition: 'slide', // fade, slide or plain
        allowToastClose: true, // Boolean value true or false
        hideAfter: 7000, // false to make it sticky or number representing the miliseconds as time after which toast needs to be hidden
        stack: 5, // false if there should be only one toast at a time or a number representing the maximum number of toasts to be shown at a time

        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 }, // bottom-left or bottom-right or bottom-center or top-left or top-right or top-center or mid-center or an object representing the left, right, top, bottom values


        textAlign: 'left', // Text alignment i.e. left, right or center
        loader: true, // Whether to show loader or not. True by default
        loaderBg: '#e22d2d', // Background color of the toast loader




    });
    clearInterval(myVar5);
}



var myVar6 = setInterval(myTimer6, 55000);

function myTimer6() {
    $.toast({
        text: "26m yang lalu memesan paket Sgrow", // Text that is to be shown in the toast
        heading: 'Aditya Hendra di Makasar', // Optional heading to be shown on the toast
        icon: 'success', // Type of toast icon
        showHideTransition: 'slide', // fade, slide or plain
        allowToastClose: true, // Boolean value true or false
        hideAfter: 7000, // false to make it sticky or number representing the miliseconds as time after which toast needs to be hidden
        stack: 5, // false if there should be only one toast at a time or a number representing the maximum number of toasts to be shown at a time

        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 }, // bottom-left or bottom-right or bottom-center or top-left or top-right or top-center or mid-center or an object representing the left, right, top, bottom values


        textAlign: 'left', // Text alignment i.e. left, right or center
        loader: true, // Whether to show loader or not. True by default
        loaderBg: '#e22d2d', // Background color of the toast loader




    });
    clearInterval(myVar6);
}




var myVar7 = setInterval(myTimer7, 65000);

function myTimer7() {
    $.toast({
        text: "30m yang lalu memesan paket Sgrow",
        heading: 'Wafa Rifki di Bekasi',
        icon: 'success',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar7);
}



var myVar8 = setInterval(myTimer8, 75000);

function myTimer8() {
    $.toast({
        text: "34m yang lalu memesan paket Sgrow",
        heading: 'Siti Nurhaliza di Boyolali',
        icon: 'success2',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar8);
}



var myVar9 = setInterval(myTimer9, 85000);

function myTimer9() {
    $.toast({
        text: "37m yang lalu memesan paket Sgrow",
        heading: 'Syifa Yolanda di Madiun',
        icon: 'success2',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar9);
}



var myVar10 = setInterval(myTimer10, 95000);

function myTimer10() {
    $.toast({
        text: "Baru Saja memesan paket Sgrow",
        heading: 'Darryl Steven di Jakarta',
        icon: 'success',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar10);
}




var myVar11 = setInterval(myTimer11, 105000);

function myTimer11() {
    $.toast({
        text: "14m yang lalu memesan paket Sgrow",
        heading: 'Devi Azzahra di Padang',
        icon: 'success2',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar11);
}



var myVar12 = setInterval(myTimer12, 115000);

function myTimer12() {
    $.toast({
        text: "23m yang lalu memesan paket Sgrow",
        heading: 'Handoko di Slawi',
        icon: 'success',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar12);
}



var myVar13 = setInterval(myTimer13, 125000);

function myTimer13() {
    $.toast({
        text: "37m yang lalu memesan paket Sgrow",
        heading: 'Sudibyo di Solo',
        icon: 'success',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar13);
}



var myVar14 = setInterval(myTimer14, 125000);

function myTimer14() {
    $.toast({
        text: "48m yang lalu memesan paket Sgrow",
        heading: 'Laela Nuryani di jombang',
        icon: 'success2',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar14);
}



var myVar15 = setInterval(myTimer15, 135000);

function myTimer15() {
    $.toast({
        text: "5m yang lalu memesan paket Sgrow",
        heading: 'Nurhalifa di Tangerang',
        icon: 'success2',
        showHideTransition: 'slide',
        allowToastClose: true,
        hideAfter: 7000,
        stack: 5,
        position: { left: 'auto', right: 30, top: 'auto', bottom: 30 },
        textAlign: 'left',
        loader: true,
        loaderBg: '#e22d2d',
    });
    clearInterval(myVar15);
}